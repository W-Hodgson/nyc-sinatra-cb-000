<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>New Figure</title>
  </head>
  <body>
    <form action="/figures" method="post">
      <p>Name: <input type="text" name="figure[name]" value=""></p>

      <h3>Titles</h3> <br>
      <% @titles.each do |title|%>
        <input type="checkbox" name="figure[title_ids][]" id="<%= title.id %>" value="<%= title.id %>"><%= title.name %> <br>
      <% end %>

      <h3>Create New Title:</h3>
      <p><input type="text" name="title[name]" id="title_name"></p>

      <h3>Landmarks</h3>
      <% @landmarks.each do |landmark| %>
        <input type="checkbox" name="figure[landmark_ids][]" id="<%= landmark.id %>" value="<%= landmark.id %>"><%= %> <br>
      <% end %>

      <input type="submit" name="" value="Create Figure">
    </form>
  </body>
</html>
